model Recipe {
    id               String             @id @default(auto()) @map("_id") @db.ObjectId
    name             String
    cookingTime      Int
    numberOfServings Int
    ingredients      RecipeIngredient[]
    steps            RecipeStep[]
}
